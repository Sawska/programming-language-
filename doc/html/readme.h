/**
 * @mainpage My Project Documentation
 *
 * @section intro_sec Introduction
 *
 * This is the main page of the documentation for my programming language.
 * It is a C++ project that includes a lexer, parser, and compiler.
 *
 * @section install_sec Installation
 *
 * To install the project, follow these steps:
 * - Step 1: Download the source code.
 * - Step 2: Compile the project using CMake.
 * - Step 3: Run the compiled binary.
 *
 * @section usage_sec Usage
 *
 * Here’s how to use the project:
 * 1. Run the lexer.
 * 2. Parse the input.
 * 3. Compile and generate the output.
 *
 * @section syntax_sec Syntax of the Language
 * 
 * ### Supported Operations
 * 
 * Operators:
 * - `+`, `-`, `++`, `--`, `/`, `?`, `>>=`, `<<=`, `*`
 * 
 * ### How to Initialize a Variable
 * ```
 * let a = 1;
 * ```
 * 
 * ### How to Initialize a Function
 * ```
 * function name() {
 *   // body of the function
 * }
 * ```
 * 
 * ### Control Flow
 * - `while` loop:
 *   ```
 *   while (statement) {
 *       // body of the loop
 *   }
 *   ```
 * - `for` loop:
 *   ```
 *   for (let i = 0; i < 5; i++) {
 *       // loop body
 *   }
 *   ```
 * - `if`, `else if`, `else`:
 *   ```
 *   if (statement) {
 *       // code
 *   } else if (statement) {
 *       // code
 *   } else {
 *       // code
 *   }
 *   ```
 * 
 * ### Object-Oriented Programming (OOP)
 * - Define a class:
 *   ```
 *   class Name {
 *       property;
 *       
 *       method() {
 *           // method body
 *       }
 *   }
 *   ```
 * - Instantiate an object and access properties/methods:
 *   ```
 *   let b = new Name();
 *   b.property;
 *   b.method();
 *   ```
 * 
 * ### Supported Keywords
 * - `return`
 * - `break`
 * - `continue`
 *
 * @section more_info_sec More Information
 *
 * For more details, check out the full documentation of each module.
 */
